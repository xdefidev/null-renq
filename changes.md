# removed the <php> section from the index.php file and converted it to a .html to ease the working process, please put it back
# created a modalStyle.css file and included it in index.html (styles section). this file includes styles for : modal, modal cloes button
# some styles are declared inside the index.html
# in walletController.js file added multiple functions and documented eachone
# one other change that we made was changing walletconnect to cache the provider